header
  width 100%
  height 60px
  background-color $primary
  position fixed
  top 0
  z-index $header

  @media (min-width $minDesk)
    height 70px

    img
      65px

  > div
    color $white
    display flex
    height 100%
    justify-content space-between
    align-items center
    padding 0 $m

    img
      height 50px

    span:before
      font-size 1.5rem
    